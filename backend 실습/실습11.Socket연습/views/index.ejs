<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <script src="https://cdn.socket.io/4.5.0/socket.io.min.js" integrity="sha384-7EyYLQZgWBi67fBtVxw60/OWl1kjsfrPFcaU0pp0nAh+i8FD068QogUvg85Ewy1k" crossorigin="anonymous"></script>
  <script src="https://code.jquery.com/jquery-3.6.1.min.js" integrity="sha256-o88AwQnZB+VDvE9tvIXrMQaPlFFSUTR+nldQm1LuPXQ=" crossorigin="anonymous"></script>
</head>

<body>
  <!-- <div id="chatContent">

  </div>
  <input type="text" id="myChat"> -->

  <H1>Hello World!</H1>
  <button id="btn1" type="button" value="hello">hello</button>
  <button id="btn2" type="button" value="study">study</button>
  <button id="btn3" type="button" value="bye">bye</button>
  <div id="space"></div>

  <script>
    var socket = io.connect();

    $("button").on("click", function() {
      if ($(this).val() == "hello") {
        $("#space").append(`${$(this).val()} : 안뇽 ! <br>`)
      } else if ($(this).val() == "study") {
        $("#space").append(`${$(this).val()} : 공부합시다 ! <br>`)
      } else if ($(this).val() == "bye") {
        $("#space").append(`${$(this).val()} : 잘있어 ! <br>`);
      }

      socket.emit('msg', $(this).val());
    });

    // socket.on('msg', function(data) {
    //   $("#chatContent").append(`${data}<br>`);
    // });

    // $("#myChat").on("keyup", function() {
    //   if (window.event.keyCode == 13) {
    //     $("#chatContent").append(`Client : "${$(this).val()}" 보냅니다.<br>`);
    //     socket.emit('msg', $(this).val());
    //     $(this).val("");
    //   }
    // });
  </script>
</body>

</html>